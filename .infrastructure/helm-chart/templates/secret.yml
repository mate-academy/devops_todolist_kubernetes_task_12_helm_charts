apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secrets
  namespace: {{ .Values.namespace.todoapp.name }}
type: Opaque
data:
{{- range $key, $val := .Values.todoapp.secret }}
  {{ $key }}: {{ $val }}
{{- end -}}

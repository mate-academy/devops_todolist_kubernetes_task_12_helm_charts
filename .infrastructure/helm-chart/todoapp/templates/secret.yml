apiVersion: v1
kind: Secret
metadata:
    name: {{ .Chart.Name }}-secrets
    namespace: {{ .Values.namespace.todoapp }}
type: Opaque
data:
{{- range $key, $val := .Values.todoapp.secret }}
    {{ $key | upper }}: {{ $val | b64enc | quote}}
{{- end }}